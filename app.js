
const ICONS = {dashboard:"◫",book:"📘",assess:"🛡",quiz:"🏆",search:"⌕",activity:"🕘",cert:"🏅",users:"👥",tree:"⎇",upload:"⇪",file:"📄",report:"📊",settings:"⚙",logout:"↩"};
const state={user:null,screen:"dashboard",selectedDocument:null,selectedModule:null,selectedAssessment:null,searchQuery:"",selectedBranch:null,searchRun:1,copiedId:"",registryFilter:"All",uploadTab:"content",uploadForm:{title:"",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Refunds",taskType:"Payment Query",keywords:"",summary:"",type:"SOP",uploadKind:"Video",version:"1.0"},quizSession:null,assessmentSelection:"",assessmentSubmitted:false};
const users=[{id:"EMP001",name:"Amina Rahman",password:"demo",role:"learner",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Refunds",taskType:"Payment Query",completedModules:["MOD001"],completedAssessments:["ASM001"]},{id:"ADM001",name:"Hassan Ali",password:"demo",role:"admin",entity:"Meydan",lob:"All",subLob:"All",service:"All",taskType:"All",completedModules:[],completedAssessments:[]}];
const documents=[{id:"MEY-CX-INB-REF-0007",title:"Refund Handling for Duplicate Payment",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Refunds",taskType:"Payment Query",type:"SOP",version:"1.2",owner:"Operations QA",updatedAt:"30 Mar 2026",status:"active",keywords:["refund","duplicate payment","double debit","reversal"],summary:"Steps to validate duplicate payment complaints, confirm transaction trail, and advise refund timelines."},{id:"MEY-CX-INB-CAN-0011",title:"Cancellation Request Process",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Retention",taskType:"Cancellation",type:"Process Note",version:"1.0",owner:"Customer Experience",updatedAt:"29 Mar 2026",status:"active",keywords:["cancel","cancellation","close account","retention"],summary:"Guidance for handling customer cancellation intent, retention script usage, and closure documentation."},{id:"MEY-CX-INB-KYC-0003",title:"KYC Mismatch Escalation Guide",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Application Support",taskType:"KYC Escalation",type:"FAQ",version:"2.1",owner:"Compliance Desk",updatedAt:"28 Mar 2026",status:"active",keywords:["kyc","mismatch","application","rejected","escalation"],summary:"Checklist for document mismatch review, accepted proofs, and escalation workflow to compliance teams."},{id:"MEY-AS-GUI-ONB-0004",title:"Guide Onboarding Checklist",entity:"Meydan",lob:"AS",subLob:"Guide",service:"Onboarding",taskType:"New Joiner Setup",type:"PDF",version:"1.0",owner:"AS Operations",updatedAt:"27 Mar 2026",status:"active",keywords:["guide","onboarding","setup","joining"],summary:"Checklist for guide onboarding, access setup, and first-day task readiness."},{id:"MEY-CS-CS3-QRY-0009",title:"CS 3 Query Handling Matrix",entity:"Meydan",lob:"CS",subLob:"CS 3",service:"Customer Queries",taskType:"Escalation Review",type:"SOP",version:"1.1",owner:"CS Quality",updatedAt:"26 Mar 2026",status:"active",keywords:["cs3","query","escalation","customer issue"],summary:"Escalation matrix and ownership model for CS 3 customer query handling."},{id:"SPC-CX-INB-REF-0002",title:"SPC Refund Process",entity:"SPC",lob:"CX",subLob:"Inbound",service:"Refunds",taskType:"Payment Query",type:"SOP",version:"1.0",owner:"SPC Ops",updatedAt:"30 Mar 2026",status:"active",keywords:["refund","duplicate payment"],summary:"SPC-only refund flow."}];
const modules=[{id:"MOD001",title:"Inbound Refund Basics",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Refunds",taskType:"",mandatory:true,type:"Video",duration:"18 min",progress:100,order:1,linkedAssessmentId:"ASM001"},{id:"MOD002",title:"Payment Query Documentation Standards",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Refunds",taskType:"Payment Query",mandatory:true,type:"PDF",duration:"12 min",progress:40,order:2,linkedAssessmentId:"ASM002"},{id:"MOD003",title:"Customer Communication Essentials",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"",taskType:"",mandatory:true,type:"Interactive",duration:"24 min",progress:0,order:3,linkedAssessmentId:"ASM003"}];
const processDocuments=[{id:"PROC-MEY-CX-001",title:"Refund Escalation Process Manual",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Refunds",taskType:"Payment Query",version:"1.0",status:"active",acknowledgementRequired:true},{id:"PROC-MEY-CX-002",title:"KYC Resolution Process Note",entity:"Meydan",lob:"CX",subLob:"Inbound",service:"Application Support",taskType:"KYC Escalation",version:"1.3",status:"active",acknowledgementRequired:true}];
const acknowledgements=[{processId:"PROC-MEY-CX-001",userId:"EMP001",acknowledgedAt:"31 Mar 2026, 10:20",status:"Acknowledged"}];
const assessments=[{id:"ASM001",title:"Inbound Refund Assessment",moduleId:"MOD001",questions:20,pass:80,status:"Completed"},{id:"ASM002",title:"Communication Standards Check",moduleId:"MOD002",questions:15,pass:85,status:"Pending"},{id:"ASM003",title:"Customer Communication Mastery",moduleId:"MOD003",questions:12,pass:80,status:"Locked"}];
const quizSets=[{id:"Q001",title:"Refund Quick Challenge",xp:50,status:"Ready",questions:[{q:"What should be verified first in a duplicate payment case?",options:["Customer birthday","Transaction trail","Cancellation status","Supervisor availability"],answer:"Transaction trail"},{q:"Which field must be captured for CRM compliance?",options:["Document ID","Mood tag","Call colour","Ticket emoji"],answer:"Document ID"}]},{id:"Q002",title:"CX Knowledge Sprint",xp:30,status:"Ready",questions:[{q:"Entity-locked search should return results from:",options:["All entities","Logged-in entity only","Admin entity only","Any active document"],answer:"Logged-in entity only"}]}];
const taxonomy=[{entity:"Meydan",lobs:[{name:"CX",subLobs:[{name:"Inbound",services:["Refunds","Retention","Application Support"]}]},{name:"AS",subLobs:[{name:"Guide",services:["Onboarding"]}]},{name:"CS",subLobs:[{name:"CS 3",services:["Customer Queries"]}]}]},{entity:"SPC",lobs:[{name:"CX",subLobs:[{name:"Inbound",services:["Refunds"]}]}]},{entity:"New Entity Template",lobs:[{name:"LOB Placeholder",subLobs:[{name:"Sub-LOB Placeholder",services:["Service Placeholder"]}]}]}];
function getScopedModules(user){return modules.filter(m=>m.entity===user.entity&&(user.role==="admin"||m.lob===user.lob)).sort((a,b)=>a.order-b.order);}
function isModuleUnlocked(module,user){const scoped=getScopedModules(user); if(module.order===1) return true; const prev=scoped.find(m=>m.order===module.order-1); if(!prev) return true; const prevAsm=assessments.find(a=>a.moduleId===prev.id); return user.completedModules.includes(prev.id)&&prevAsm&&user.completedAssessments.includes(prevAsm.id);}
function scoreDocument(doc,query,user){const q=query.toLowerCase().trim(); if(!q) return 0; const terms=q.split(/\s+/).filter(Boolean); let score=0; if(doc.entity!==user.entity||doc.status!=="active") return -1; const hay={taskType:doc.taskType.toLowerCase(),service:doc.service.toLowerCase(),subLob:doc.subLob.toLowerCase(),lob:doc.lob.toLowerCase(),title:doc.title.toLowerCase(),keywords:doc.keywords.join(" ").toLowerCase(),summary:doc.summary.toLowerCase()}; terms.forEach(term=>{if(hay.taskType.includes(term)) score+=60; if(hay.service.includes(term)) score+=45; if(hay.subLob.includes(term)) score+=30; if(hay.lob.includes(term)) score+=20; if(hay.title.includes(term)) score+=18; if(hay.keywords.includes(term)) score+=14; if(hay.summary.includes(term)) score+=8;}); return score;}
function suggestRelevantBranches(query,user){const q=query.toLowerCase().trim(); if(!q) return []; const terms=q.split(/\s+/).filter(Boolean); const scopedDocs=documents.filter(doc=>doc.entity===user.entity); const branchMap=new Map(); scopedDocs.forEach(doc=>{let branchScore=0; const combined=[doc.lob,doc.subLob,doc.service,doc.taskType,doc.title,doc.keywords.join(" ")].join(" ").toLowerCase(); terms.forEach(term=>{if(combined.includes(term)) branchScore+=1;}); if(branchScore>0){const key=doc.lob+"|||"+doc.subLob; const existing=branchMap.get(key)||{lob:doc.lob,subLob:doc.subLob,score:0}; existing.score+=branchScore; branchMap.set(key,existing);}}); return Array.from(branchMap.values()).sort((a,b)=>b.score-a.score).slice(0,6);}
function icon(name){return `<span>${ICONS[name]||"•"}</span>`;}
function logoMarkup(){return `<div class="logo-wrap"><div style="width:58px;height:58px;border-radius:50%;background:#eaf3fb;color:#0d4b7d;display:flex;align-items:center;justify-content:center;font-weight:700;">O</div><div class="logo-word">cube</div></div>`;}
function formatScope(user){return `${user.entity} > ${user.lob} > ${user.subLob} > ${user.service} > ${user.taskType}`;}
function escapeHtml(str){return String(str).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;");}
function escapeAttr(str){return escapeHtml(str).replace(/"/g,"&quot;");}
function field(label,key,value,extra=""){return `<div class="field ${extra}"><label>${label}</label><input data-upload-field="${key}" value="${escapeAttr(value||"")}"></div>`;}
function textareaField(label,key,value,extra=""){return `<div class="field ${extra}"><label>${label}</label><textarea rows="4" data-upload-field="${key}">${escapeHtml(value||"")}</textarea></div>`;}
function selectField(label,key,options,value){return `<div class="field"><label>${label}</label><select data-upload-field="${key}">${options.map(opt=>`<option ${opt===value?'selected':''}>${opt}</option>`).join("")}</select></div>`;}
function statCard(title,value,sub,emoji){return `<div class="card small-pad"><div class="flex-between"><div><div class="eyebrow" style="letter-spacing:.2em;">${title}</div><div class="title-xl">${value}</div><div class="sub">${sub}</div></div><div style="width:48px;height:48px;border-radius:18px;background:var(--blue-soft);display:flex;align-items:center;justify-content:center;font-size:22px;color:var(--blue)">${emoji}</div></div></div>`;}
function topbar(){return `<div class="topbar"><div><div class="eyebrow">Ocube training academy</div><div class="title-xl" style="font-size:36px;">Welcome, ${state.user.name}</div><div class="sub">${state.user.entity} • ${state.user.lob} • ${state.user.subLob}</div></div><div class="scope-pill"><strong>Scope:</strong> ${formatScope(state.user)}</div></div>`;}
function sidebarItems(){if(state.user.role==="admin"){return [["dashboard","Admin Dashboard","dashboard"],["taxonomy","Taxonomy Manager","tree"],["users","User Management","users"],["upload","Content Upload Center","upload"],["registry","Document Registry","file"],["analytics","Search Analytics","search"],["reports","Reports","report"],["settings","Settings","settings"]];} return [["dashboard","Dashboard","dashboard"],["learning","My Learning Path","book"],["assessments","Assessments","assess"],["quiz","Gamified Quiz","quiz"],["search","Decision Tree Search","search"],["activity","My Activity","activity"],["certificates","Certificates","cert"],["profile","Profile","users"]];}
function generatedId(){const f=state.uploadForm; return `${f.entity==="Meydan"?"MEY":"SPC"}-${f.lob.slice(0,2).toUpperCase()}-${f.subLob.slice(0,3).toUpperCase()}-${f.service.slice(0,3).toUpperCase()}-NEW1`;}
function render(){document.getElementById("app").innerHTML = state.user ? renderPortal() : renderLogin(); bindEvents();}
function renderLogin(){return `<div class="login-shell"><div class="login-grid"><div class="login-hero"><div>${logoMarkup()}</div><div style="max-width:640px;"><div class="eyebrow" style="color:rgba(255,255,255,.76)">Training portal</div><div style="font-size:54px;font-weight:700;line-height:1.08;margin-top:18px;">REIMAGINE LEARNING WITH OCUBE.</div><div style="font-size:16px;line-height:1.8;margin-top:18px;color:rgba(255,255,255,.82);max-width:560px;">A taxonomy-driven LMS for entity-locked learning, cross-LOB entity search, dynamic search references, assessments, and gamified knowledge delivery.</div></div><div class="hero-badges"><div class="hero-badge"><div style="font-size:26px;font-weight:700;">Entity-locked</div><div style="margin-top:6px;color:rgba(255,255,255,.78);font-size:14px;">Secure content visibility.</div></div><div class="hero-badge"><div style="font-size:26px;font-weight:700;">Dynamic IDs</div><div style="margin-top:6px;color:rgba(255,255,255,.78);font-size:14px;">Search-session reference output.</div></div><div class="hero-badge"><div style="font-size:26px;font-weight:700;">Scale ready</div><div style="margin-top:6px;color:rgba(255,255,255,.78);font-size:14px;">1,500 employees / 1,000+ searches.</div></div></div></div><div class="login-panel"><div class="card login-card"><div class="eyebrow">Sign in</div><div class="title-xl">Access your portal</div><div class="sub">Use the demo credentials to preview the learner or admin experience.</div><div class="stack" style="margin-top:24px;"><div class="field"><label>Employee ID</label><input id="login-employee" value="EMP001"></div><div class="field"><label>Password</label><input id="login-password" type="password" value="demo"></div><div id="login-error"></div><button class="btn" id="login-submit">Sign in</button><div class="helper-box">Learner demo: <strong>EMP001 / demo</strong><br>Admin demo: <strong>ADM001 / demo</strong></div></div></div></div></div></div>`;}
function renderPortal(){return `<div class="app-shell"><div class="layout"><aside class="sidebar">${logoMarkup()}<div class="sidebar-nav">${sidebarItems().map(([key,label,ic])=>`<button class="nav-btn ${state.screen===key?'active':''}" data-nav="${key}">${icon(ic)} <span>${label}</span></button>`).join("")}</div><div class="sidebar-footer"><button class="secondary-btn" id="logout-btn">${icon("logout")} Logout</button></div></aside><main class="main stack">${renderScreen()}</main></div></div>`;}
function renderLearnerDashboard(){const scoped=getScopedModules(state.user); return `${topbar()}<div class="grid-4">${statCard("Assigned Modules",scoped.length,"Across your full LOB","📘")}${statCard("Completed",state.user.completedModules.length,"Completed learning units","✅")}${statCard("Pending Assessments",assessments.filter(a=>!state.user.completedAssessments.includes(a.id)&&a.moduleId).length,"Required before next unlock","🛡")}${statCard("Search Capacity","1000+","Searches per day ready","📊")}</div><div class="grid-main"><div class="card"><div class="flex-between" style="margin-bottom:18px;"><div><div class="eyebrow">Continue learning</div><div class="title-lg">Your LOB-mapped modules</div></div><button class="btn" data-action="go-search">Search support docs</button></div><div class="stack" style="gap:16px;">${scoped.map(m=>{const unlocked=isModuleUnlocked(m,state.user); return `<div class="module-card"><div class="flex-between" style="align-items:flex-start;"><div><div class="title-md">${m.title}</div><div class="sub">${m.type} • ${m.duration} • ${m.mandatory?"Mandatory":"Optional"}</div><div class="simple-note" style="margin-top:8px;color:${unlocked?'var(--blue)':'var(--locked)'}">${unlocked?"Unlocked":"Complete previous module and assessment first"}</div></div><button class="btn btn-soft" ${unlocked?"":"disabled"} data-open-module="${m.id}">${unlocked?"Resume":"Locked"}</button></div><div class="progress" style="margin-top:16px;"><span style="width:${m.progress}%"></span></div><div class="sub" style="margin-top:10px;">${m.progress}% complete</div></div>`;}).join("")}</div></div><div class="stack"><div class="card"><div class="eyebrow">Quick search</div><div class="title-lg">Decision tree</div><div class="sub">Search across all LOB and sub-LOB branches within your logged-in entity only. Ranking prioritises task type, then service, sub-LOB, and LOB.</div><button class="btn" style="width:100%;margin-top:18px;" data-action="go-search">Open decision tree search</button></div><div class="card"><div class="eyebrow">Scale readiness</div><div class="sub" style="margin-top:10px;">Designed to support about 1,500 employees, average daily footfall around 700 users, and 1,000+ searches per day using lightweight metadata-based ranking scoped to entity only.</div></div></div></div>`;}
function renderSearch(){const branches=suggestRelevantBranches(state.searchQuery,state.user); const results=documents.map(d=>({...d,score:scoreDocument(d,state.searchQuery,state.user)})).filter(d=>d.score>0).sort((a,b)=>b.score-a.score).filter(d=>!state.selectedBranch||(d.lob===state.selectedBranch.lob&&d.subLob===state.selectedBranch.subLob)).map((d,idx)=>({...d,searchInstanceId:`${d.id}-SRCH-${state.searchRun}-${idx+1}`})); return `${topbar()}<div class="card"><div class="eyebrow">Decision tree search</div><div class="title-xl">Natural-language document retrieval</div><div class="sub">Searching within: ${state.user.entity} only</div><div class="flex-between" style="align-items:stretch;margin-top:18px;"><div class="search-wrap" style="flex:1;"><span class="search-icon">⌕</span><input id="dt-query" autocomplete="off" placeholder="Search by customer issue, service, task type, or process language" value="${escapeHtml(state.searchQuery)}"></div><button class="btn" id="dt-search-btn" style="min-width:140px;">Search</button></div></div>${branches.length?`<div class="card"><div class="eyebrow">Suggested LOB / Sub-LOB branches</div><div class="sub">Based on query keywords, choose the most relevant branch to narrow the solution path.</div><div class="chips" style="margin-top:16px;">${branches.map(b=>`<button class="chip" data-branch="${b.lob}|||${b.subLob}" style="${state.selectedBranch&&state.selectedBranch.lob===b.lob&&state.selectedBranch.subLob===b.subLob?'background:var(--blue);color:white;':''}">${b.lob} / ${b.subLob}</button>`).join("")}${state.selectedBranch?`<button class="chip" id="clear-branch">Clear selection</button>`:""}</div></div>`:""}<div class="grid-main"><div class="stack" style="gap:16px;">${results.length===0?`<div class="card"><div class="sub">No active matches found in ${state.user.entity}. Try service name, issue type, LOB, sub-LOB, or task keyword.</div></div>`:""}${results.map((doc,idx)=>`<div class="card"><div class="flex-between" style="align-items:flex-start;"><div><div class="badge ${idx===0?'badge-blue':'badge-grey'}">${idx===0?'Best Match':'Related Match'}</div><div class="title-lg" style="margin-top:10px;">${doc.title}</div><div style="margin-top:10px;color:var(--blue);font-weight:700;font-size:14px;">${doc.searchInstanceId}</div><div class="simple-note">Base document: ${doc.id}</div><div class="sub">${doc.entity} > ${doc.lob} > ${doc.subLob} > ${doc.service} > ${doc.taskType}</div><div class="sub">${doc.summary}</div><div class="simple-note">Version ${doc.version} • Updated ${doc.updatedAt} • Owner ${doc.owner}</div></div><div style="display:flex;gap:8px;flex-wrap:wrap;"><button class="btn btn-soft" data-open-doc="${doc.id}" data-search-id="${doc.searchInstanceId}">Open</button><button class="btn" data-copy-id="${doc.searchInstanceId}">Copy ID</button></div></div>${state.copiedId===doc.searchInstanceId?`<div style="margin-top:12px;color:var(--blue);font-size:14px;">Search reference ID copied for CRM internal remarks.</div>`:""}</div>`).join("")}</div><div class="stack"><div class="card"><div class="eyebrow">Search rules</div><div class="sub" style="margin-top:10px;">• Locked to logged-in entity only.<br>• Ranking priority: task type, service, sub-LOB, then LOB within the same entity.<br>• Every result gets a fresh search reference ID per search run.<br>• Designed for CRM internal remarks referencing.</div></div><div class="card"><div class="eyebrow">Volume readiness</div><div class="sub" style="margin-top:10px;">This decision tree structure is intended to support about 700 daily active users and 1,000+ searches per day by staying entity-scoped and metadata-driven.</div></div></div></div>`;}
function renderLearningPath(){const scoped=getScopedModules(state.user); return `${topbar()}<div class="card"><div class="eyebrow">My learning path</div><div class="title-xl">Mapped training modules</div><div class="sub">${state.user.entity} > ${state.user.lob} > Sequential progression enabled</div></div><div class="grid-2">${scoped.map(m=>{const unlocked=isModuleUnlocked(m,state.user); const linked=assessments.find(a=>a.id===m.linkedAssessmentId); return `<div class="card"><div class="flex-between" style="align-items:flex-start;"><div><div class="title-md">${m.title}</div><div class="sub">${m.type} • ${m.duration} • ${m.mandatory?"Mandatory":"Optional"}</div><div class="simple-note" style="margin-top:8px;color:${unlocked?'var(--success)':'var(--locked)'}">${unlocked?"Ready to start":"Locked until previous module and assessment are completed"}</div><div class="simple-note">Linked assessment: ${linked?linked.title:"Not assigned"}</div></div><span class="badge badge-blue">${m.progress}%</span></div><div class="progress" style="margin-top:16px;"><span style="width:${m.progress}%"></span></div><div class="flex-between" style="margin-top:16px;font-size:14px;color:#64748b;"><span>${unlocked?"Resume learning":"Locked"}</span><button class="btn btn-soft" ${unlocked?"":"disabled"} data-open-module="${m.id}">${unlocked?"Open":"Locked"}</button></div></div>`;}).join("")}</div>`;}
function renderModuleDetail(){const m=state.selectedModule; if(!m) return `<div class="card"><div class="sub">Select a module to review lesson structure and linked learning assets.</div></div>`; const linked=assessments.find(a=>a.id===m.linkedAssessmentId); const unlocked=isModuleUnlocked(m,state.user); return `<div class="stack"><div class="card"><div class="flex-between" style="align-items:flex-start;"><div><div class="eyebrow">Module detail</div><div class="title-xl">${m.title}</div><div class="sub">${m.type} • ${m.duration} • ${m.mandatory?"Mandatory":"Optional"}</div><div class="simple-note" style="margin-top:8px;color:${unlocked?'var(--success)':'var(--locked)'}">${unlocked?"This module is unlocked.":"This module is locked until the previous module and assessment are completed."}</div></div><button class="btn btn-soft" id="back-learning">Back to learning path</button></div></div><div class="grid-module"><div class="card"><div class="eyebrow">Lessons</div><div class="stack" style="margin-top:16px;gap:12px;">${["Introduction","Process walkthrough","Knowledge check","Wrap-up"].map((lesson,i)=>`<div class="list-card" style="display:flex;justify-content:space-between;align-items:center;"><div><div class="title-sm">Lesson ${i+1}</div><div class="sub">${lesson}</div></div><div style="font-size:20px;color:${i<2?"var(--blue)":"#cbd5e1"}">✓</div></div>`).join("")}</div></div><div class="card"><div class="eyebrow">Learning area</div><div class="list-card" style="margin-top:16px;background:#f8fafc;"><div class="title-md">Interactive content placeholder</div><div class="sub">This section will render the actual lesson viewer, simulation, video, or PDF content. The learner must complete this module and its linked assessment before the next module becomes available.</div><div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:18px;"><button class="btn">Mark lesson complete</button><button class="btn btn-soft" ${linked?`data-open-assessment="${linked.id}"`:""}>Open linked assessment</button></div></div></div></div></div>`;}
function renderAssessments(){const scopedMods=getScopedModules(state.user); const scopedAssessments=assessments.filter(a=>scopedMods.some(m=>m.id===a.moduleId)); return `<div class="stack"><div class="card"><div class="eyebrow">Assessments</div><div class="title-xl">Role-mapped checks</div></div><div class="grid-2">${scopedAssessments.map(a=>{const module=modules.find(m=>m.id===a.moduleId); const unlocked=module?isModuleUnlocked(module,state.user):true; const completed=state.user.completedAssessments.includes(a.id); return `<div class="card"><div class="flex-between" style="align-items:flex-start;"><div><div class="title-md">${a.title}</div><div class="sub">${a.questions} questions • Pass mark ${a.pass}%</div><div class="simple-note" style="margin-top:8px;color:${completed?'var(--success)':unlocked?'var(--blue)':'var(--locked)'}">${completed?"Completed":unlocked?"Available":"Locked"}</div></div><span class="badge badge-grey">${completed?"Completed":unlocked?"Pending":"Locked"}</span></div><div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:18px;"><button class="btn" ${(!unlocked||completed)?"disabled":""} data-start-assessment="${a.id}">Start assessment</button><button class="btn btn-soft">Review scope</button></div></div>`;}).join("")}</div></div>`;}
function renderAssessmentAttempt(){const a=state.selectedAssessment||{title:"Assessment",questions:20}; const question={title:"Which action should be taken first for a duplicate payment refund query?",options:["Close the case immediately","Validate the transaction trail and customer details","Offer cancellation workflow","Escalate without verification"],answer:"Validate the transaction trail and customer details"}; return `<div class="stack"><div class="card"><div class="flex-between"><div><div class="eyebrow">Assessment attempt</div><div class="title-xl">${a.title}</div></div><button class="btn btn-soft" id="back-assessments">Back</button></div></div><div class="card"><div class="title-sm" style="color:#64748b;">Question 1 of ${a.questions}</div><div class="title-lg" style="margin-top:10px;">${question.title}</div><div class="stack" style="gap:12px;margin-top:18px;">${question.options.map(opt=>`<button class="btn-outline assessment-option" data-assessment-option="${escapeAttr(opt)}" style="text-align:left;border-radius:18px;padding:16px;border:1px solid ${state.assessmentSelection===opt?'var(--blue)':'var(--border)'};background:${state.assessmentSelection===opt?'var(--blue-soft)':'white'};">${opt}</button>`).join("")}</div><div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:18px;"><button class="btn" id="submit-assessment-answer">Submit answer</button><button class="btn btn-soft">Next question</button></div>${state.assessmentSubmitted?`<div class="list-card" style="margin-top:16px;background:${state.assessmentSelection===question.answer?'var(--success-soft)':'var(--warning-soft)'};color:${state.assessmentSelection===question.answer?'var(--success)':'var(--warning)'};">${state.assessmentSelection===question.answer?'Correct. This reflects the approved refund handling flow.':'Not quite. The first step is to validate the transaction trail and customer details.'}</div>`:""}</div></div>`;}
function renderQuiz(){if(!state.quizSession){return `<div class="stack"><div class="card"><div class="eyebrow">Gamified quiz</div><div class="title-xl">Knowledge reinforcement</div><div class="sub">Role-mapped quiz sets designed to strengthen retention through short challenges and score-based rewards.</div></div><div class="grid-2">${quizSets.map(q=>`<div class="card"><div class="flex-between" style="align-items:flex-start;"><div><div class="title-md">${q.title}</div><div class="sub">Reward: ${q.xp} XP • ${q.questions.length} question(s)</div></div><span class="badge badge-blue">${q.status}</span></div><button class="btn" style="margin-top:18px;" data-start-quiz="${q.id}">Start challenge</button></div>`).join("")}</div></div>`;} const s=state.quizSession; const q=s.quiz.questions[s.index]; if(s.finished){return `<div class="stack"><div class="card"><div class="eyebrow">Challenge complete</div><div class="title-xl">Score: ${s.score}/${s.quiz.questions.length}</div><div class="sub">XP earned: ${Math.round((s.score/s.quiz.questions.length)*s.quiz.xp)}</div><div class="info-box" style="margin-top:18px;"><strong style="color:var(--blue)">Badge progress updated.</strong><div class="sub">Your knowledge reinforcement score has been recorded for this mapped quiz path.</div></div><button class="btn btn-soft" style="margin-top:18px;" id="quiz-back-library">Back to quiz library</button></div></div>`;} return `<div class="stack"><div class="card"><div class="flex-between"><div><div class="eyebrow">Quiz in progress</div><div class="title-xl">${s.quiz.title}</div></div><button class="btn btn-soft" id="quiz-back-library">Back to quiz library</button></div></div><div class="card"><div class="title-sm" style="color:#64748b;">Question ${s.index+1} of ${s.quiz.questions.length}</div><div class="title-lg" style="margin-top:10px;">${q.q}</div><div class="stack" style="gap:12px;margin-top:18px;">${q.options.map(opt=>`<button class="btn-outline quiz-option" data-quiz-option="${escapeAttr(opt)}" style="text-align:left;border-radius:18px;padding:16px;border:1px solid ${s.selected===opt?'var(--blue)':'var(--border)'};background:${s.selected===opt?'var(--blue-soft)':'white'};">${opt}</button>`).join("")}</div><button class="btn" style="margin-top:18px;${s.selected?'':'opacity:.55;pointer-events:none;'}" id="quiz-next">${s.index===s.quiz.questions.length-1?'Finish challenge':'Next question'}</button></div></div>`;}
function renderActivity(){const scoped=processDocuments.filter(p=>p.entity===state.user.entity); return `<div class="stack"><div class="card"><div class="eyebrow">Process read & acknowledge</div><div class="title-xl">Mandatory process documents</div><div class="stack" style="margin-top:16px;gap:12px;">${scoped.map(proc=>{const ack=acknowledgements.find(a=>a.processId===proc.id&&a.userId===state.user.id); return `<div class="module-card"><div class="flex-between" style="align-items:flex-start;"><div><div class="title-md">${proc.title}</div><div class="sub">${proc.entity} • ${proc.lob} • ${proc.subLob} • ${proc.service} • ${proc.taskType}</div><div class="simple-note">Version ${proc.version}</div></div><button class="btn ${ack?'btn-soft':''}" style="${ack?'background:var(--success-soft);color:var(--success);':''}">${ack?'Acknowledged':'Read & acknowledge'}</button></div></div>`;}).join("")}</div></div><div class="card"><div class="eyebrow">My activity</div><div class="title-xl">Recent actions</div><div class="stack" style="gap:10px;margin-top:16px;font-size:14px;color:#475569;"><div>• Completed Communication Standards Check.</div><div>• Opened Refund Handling for Duplicate Payment.</div><div>• Copied latest search reference ID for CRM usage.</div><div>• Read and acknowledged mapped process documentation.</div></div></div></div>`;}
function renderCertificates(){return `<div class="card"><div class="eyebrow">Certificates</div><div class="title-xl">Achievements</div><div class="list-card" style="margin-top:16px;background:#f8fafc;">Certificate generation placeholder for completed pathways.</div></div>`;}
function renderProfile(){const rows=[["Employee ID",state.user.id],["Role",state.user.role],["Entity",state.user.entity],["LOB",state.user.lob],["Sub-LOB",state.user.subLob],["Service",state.user.service],["Task Type",state.user.taskType]]; return `<div class="card"><div class="eyebrow">Profile</div><div class="title-xl">Access mapping</div><div class="kv-grid" style="margin-top:18px;">${rows.map(([k,v])=>`<div class="kv-item"><div class="eyebrow" style="letter-spacing:.16em;">${k}</div><div class="title-md" style="margin-top:8px;">${v}</div></div>`).join("")}</div></div>`;}
function renderAdminDashboard(){return `<div class="stack"><div class="card"><div class="eyebrow">Admin dashboard</div><div class="title-xl">Operational control centre</div><div class="sub">Monitor mapped learners, searchable documents, content governance, taxonomy performance, and high-volume usage readiness in one place.</div></div><div class="grid-4">${statCard("Learners","1500","Planned employee capacity","👥")}${statCard("Daily Footfall","700","Average expected users","◫")}${statCard("Search Capacity","1000+","Searches per day ready","📊")}${statCard("Active Documents","326","Compliance-ready registry","📄")}</div><div class="grid-admin"><div class="card"><div class="eyebrow">Completion overview</div><div class="stack" style="gap:16px;margin-top:18px;">${[["Meydan / CX / Inbound",84],["Meydan / AS / Guide",71],["SPC / CX / Inbound",76]].map(([label,val])=>`<div><div class="flex-between" style="font-size:14px;margin-bottom:8px;"><span style="font-weight:600;">${label}</span><span class="sub">${val}%</span></div><div class="progress"><span style="width:${val}%"></span></div></div>`).join("")}</div></div><div class="card"><div class="eyebrow">Scale readiness</div><div class="stack" style="gap:10px;margin-top:16px;font-size:14px;color:#475569;"><div>• Entity-scoped search avoids cross-entity retrieval overhead.</div><div>• Metadata-driven ranking supports fast decision-tree performance.</div><div>• Branch suggestions help narrow results without full-text complexity.</div><div>• The current module is structured for about 1,500 employees, 700 daily users, and 1,000+ searches per day.</div></div></div></div></div>`;}
function renderTaxonomy(){return `<div class="card"><div class="eyebrow">Taxonomy manager</div><div class="title-xl">Entity and hierarchy structure</div><div class="stack" style="gap:16px;margin-top:18px;">${taxonomy.map(t=>`<div class="module-card"><div class="title-md">${t.entity}</div>${t.lobs.map(l=>`<div class="list-card" style="margin-top:12px;background:#f8fafc;"><div class="title-sm">LOB: ${l.name}</div>${l.subLobs.map(s=>`<div class="sub">Sub-LOB: ${s.name} • Services: ${s.services.join(", ")}</div>`).join("")}</div>`).join("")}</div>`).join("")}</div></div>`;}
function renderUsers(){const selected=state.selectedUser||users[0]; return `<div class="grid-admin"><div class="card table-wrap"><div class="eyebrow">User management</div><div class="title-xl">Mapped learner profiles</div><table style="margin-top:18px;"><thead><tr><th>Employee ID</th><th>Name</th><th>Role</th><th>Entity</th><th>LOB</th><th>Sub-LOB</th></tr></thead><tbody>${users.map(u=>`<tr data-select-user="${u.id}" style="cursor:pointer;background:${selected.id===u.id?'#f8fafc':'transparent'};"><td style="font-weight:700;">${u.id}</td><td>${u.name}</td><td style="text-transform:capitalize;">${u.role}</td><td>${u.entity}</td><td>${u.lob}</td><td>${u.subLob}</td></tr>`).join("")}</tbody></table></div><div class="card"><div class="eyebrow">Selected profile</div><div class="title-xl">${selected.name}</div><div class="stack" style="gap:10px;margin-top:18px;">${[["Employee ID",selected.id],["Role",selected.role],["Entity",selected.entity],["LOB",selected.lob],["Sub-LOB",selected.subLob],["Service",selected.service],["Task Type",selected.taskType]].map(([k,v])=>`<div class="list-card" style="background:#f8fafc;"><strong>${k}:</strong> <span class="sub">${v}</span></div>`).join("")}</div></div></div>`;}
function renderUpload(){const f=state.uploadForm; return `<div class="card"><div class="eyebrow">Content upload center</div><div class="filter-row" style="margin-top:14px;">${[["content","Training Content"],["process","Process Documents"]].map(([key,label])=>`<button class="filter-chip ${state.uploadTab===key?'active':''}" data-upload-tab="${key}">${label}</button>`).join("")}</div><div class="title-xl">Clutter-free content upload</div><div class="sub">Use this simple upload area to map videos and PDFs by entity, LOB, sub-LOB, service, and task type before they become searchable in the decision tree.</div>${state.uploadTab==="content"?`<div class="form-grid" style="margin-top:18px;">${field("Title","title",f.title)}${selectField("Entity","entity",taxonomy.map(t=>t.entity),f.entity)}${selectField("LOB","lob",["CX","AS","CS","LOB Placeholder"],f.lob)}${selectField("Sub-LOB","subLob",["Inbound","Guide","Application","CS 3","Sub-LOB Placeholder"],f.subLob)}${selectField("Service","service",["Refunds","Retention","Application Support","Onboarding","Customer Queries","Service Placeholder"],f.service)}${field("Task Type","taskType",f.taskType)}${selectField("Content Type","type",["SOP","PDF","FAQ","Process Note"],f.type)}${selectField("File Upload Type","uploadKind",["Video","PDF"],f.uploadKind)}${field("Version","version",f.version)}${field("Keywords","keywords",f.keywords,"full")}${textareaField("Summary","summary",f.summary,"full")}</div>`:`<div class="form-grid" style="margin-top:18px;">${field("Process Document Title","processTitle","","")}${selectField("Entity","processEntity",taxonomy.map(t=>t.entity),"Meydan")}${field("LOB","processLob","CX","")}${field("Sub-LOB","processSubLob","Inbound","")}${field("Service","processService","Refunds","")}${field("Task Type","processTaskType","Payment Query","")}<div class="helper-box full">Every process document can be read with acknowledgement at user level and later exported in the admin acknowledgement report.</div></div>`}<div class="grid-admin" style="margin-top:18px;"><div class="module-card"><div class="title-sm">Generated compliance template ID</div><div class="title-lg" style="margin-top:8px;color:var(--blue);">${generatedId()}</div><div class="sub">Search sessions will create fresh reference IDs when users retrieve this content.</div></div><div style="display:flex;gap:12px;align-items:center;flex-wrap:wrap;"><button class="btn">Save draft</button><button class="btn btn-soft">Validate mapping</button></div></div></div>`;}
function renderRegistry(){const rows=state.registryFilter==="All"?documents:documents.filter(d=>d.entity===state.registryFilter); return `<div class="stack"><div class="card table-wrap"><div class="flex-between"><div><div class="eyebrow">Document registry</div><div class="title-xl">Searchable compliance records</div></div><div class="filter-row">${["All",...taxonomy.map(t=>t.entity)].map(opt=>`<button class="filter-chip ${state.registryFilter===opt?'active':''}" data-registry-filter="${opt}">${opt}</button>`).join("")}</div></div><table style="margin-top:18px;"><thead><tr><th>Document ID</th><th>Title</th><th>Entity</th><th>LOB</th><th>Service</th><th>Task Type</th><th>Version</th><th>Status</th></tr></thead><tbody>${rows.map(d=>`<tr><td style="font-weight:700;color:var(--blue);">${d.id}</td><td>${d.title}</td><td>${d.entity}</td><td>${d.lob}</td><td>${d.service}</td><td>${d.taskType}</td><td>${d.version}</td><td><span class="badge badge-success">${d.status}</span></td></tr>`).join("")}</tbody></table></div><div class="card table-wrap"><div class="flex-between"><div><div class="eyebrow">Process acknowledgement report</div><div class="title-lg">User-level read acknowledgement</div></div><button class="btn" id="download-ack-report">Download acknowledgement report</button></div><table style="margin-top:18px;"><thead><tr><th>Process ID</th><th>Process Title</th><th>User</th><th>Acknowledged At</th><th>Status</th></tr></thead><tbody>${processDocuments.map(proc=>{const ack=acknowledgements.find(a=>a.processId===proc.id); return `<tr><td style="font-weight:700;color:var(--blue);">${proc.id}</td><td>${proc.title}</td><td>${ack?ack.userId:"Pending user acknowledgement"}</td><td>${ack?ack.acknowledgedAt:"—"}</td><td><span class="badge ${ack?'badge-success':'badge-warning'}">${ack?ack.status:"Pending"}</span></td></tr>`;}).join("")}</tbody></table></div></div>`;}
function renderAnalytics(){return `<div class="grid-2"><div class="card"><div class="eyebrow">Top search terms</div><div class="stack" style="gap:10px;margin-top:16px;"><div>• duplicate payment refund</div><div>• kyc mismatch</div><div>• cancellation request</div></div></div><div class="card"><div class="eyebrow">Search quality</div><div class="stack" style="gap:10px;margin-top:16px;"><div>• Success rate: 92%</div><div>• Zero-result searches: 4</div><div>• Search reference IDs are generated per run.</div><div>• Architecture target: 1,000+ searches per day.</div></div></div></div>`;}
function renderReports(){return `<div class="stack"><div class="grid-2">${statCard("Completion Rate","84%","Meydan CX Inbound","📊")}${statCard("Most Copied ID","Dynamic search reference","Fresh per search run","📋")}</div><div class="grid-2"><div class="card"><div class="eyebrow">Performance by stream</div><div class="stack" style="gap:16px;margin-top:18px;">${[["CX Inbound",88],["AS Guide",73],["CS 3",69]].map(([label,val])=>`<div><div class="flex-between" style="font-size:14px;margin-bottom:8px;"><span style="font-weight:600;">${label}</span><span class="sub">${val}%</span></div><div class="progress"><span style="width:${val}%"></span></div></div>`).join("")}</div></div><div class="card"><div class="eyebrow">Report notes</div><div class="stack" style="gap:10px;margin-top:16px;"><div>• Decision-tree usage is strongest for refund and KYC-related queries.</div><div>• Compliance references are dynamic per search session.</div><div>• Completion improvement is needed in later locked modules.</div></div></div></div></div>`;}
function renderSettings(){return `<div class="grid-2"><div class="card"><div class="eyebrow">Brand controls</div><div class="title-xl">Ocube presentation rules</div><div class="stack" style="gap:10px;margin-top:16px;"><div>• Primary colour: Ocube blue</div><div>• Typography: Open Sans styling system</div><div>• Headings in uppercase, subheadings in title case</div><div>• Professional British English voice</div></div></div><div class="card"><div class="eyebrow">Portal controls</div><div class="title-xl">Experience settings</div><div class="stack" style="gap:10px;margin-top:16px;">${["Entity-lock search enabled","Cross-LOB and sub-LOB entity search enabled","Dynamic search reference ID enabled","Sequential module progression enabled"].map(item=>`<div class="list-card" style="background:#f8fafc;display:flex;justify-content:space-between;align-items:center;"><span>${item}</span><span class="badge badge-success">ON</span></div>`).join("")}</div></div></div>`;}
function renderScreen(){if(state.user.role==="admin"){switch(state.screen){case"dashboard":return renderAdminDashboard();case"taxonomy":return renderTaxonomy();case"users":return renderUsers();case"upload":return renderUpload();case"registry":return renderRegistry();case"analytics":return renderAnalytics();case"reports":return renderReports();case"settings":return renderSettings();default:return renderAdminDashboard();}} switch(state.screen){case"dashboard":return renderLearnerDashboard();case"learning":return renderLearningPath();case"moduleDetail":return renderModuleDetail();case"assessments":return renderAssessments();case"assessmentAttempt":return renderAssessmentAttempt();case"quiz":return renderQuiz();case"search":return renderSearch();case"documentView":return renderDocumentView();case"activity":return renderActivity();case"certificates":return renderCertificates();case"profile":return renderProfile();default:return renderLearnerDashboard();}}
function bindEvents(){const loginBtn=document.getElementById("login-submit"); if(loginBtn){loginBtn.addEventListener("click",handleLogin); document.getElementById("login-employee").addEventListener("keydown",e=>{if(e.key==="Enter") handleLogin();}); document.getElementById("login-password").addEventListener("keydown",e=>{if(e.key==="Enter") handleLogin();});}
document.querySelectorAll("[data-nav]").forEach(btn=>btn.addEventListener("click",()=>{state.screen=btn.dataset.nav; if(state.screen!=="documentView") state.selectedDocument=null; if(state.screen!=="moduleDetail") state.selectedModule=null; if(state.screen!=="assessmentAttempt") state.selectedAssessment=null; render();}));
const logout=document.getElementById("logout-btn"); if(logout) logout.addEventListener("click",resetToLogin);
document.querySelectorAll("[data-action='go-search']").forEach(btn=>btn.addEventListener("click",()=>{state.screen="search"; render();}));
document.querySelectorAll("[data-open-module]").forEach(btn=>btn.addEventListener("click",()=>{state.selectedModule=modules.find(m=>m.id===btn.dataset.openModule); state.screen="moduleDetail"; render();}));
document.querySelectorAll("[data-open-doc]").forEach(btn=>btn.addEventListener("click",()=>{const doc=documents.find(d=>d.id===btn.dataset.openDoc); state.selectedDocument={...doc,searchInstanceId:btn.dataset.searchId}; state.screen="documentView"; render();}));
document.querySelectorAll("[data-copy-id]").forEach(btn=>btn.addEventListener("click",async()=>{state.copiedId=btn.dataset.copyId; try{await navigator.clipboard.writeText(btn.dataset.copyId);}catch(e){} render();}));
const backToSearch=document.getElementById("back-to-search"); if(backToSearch) backToSearch.addEventListener("click",()=>{state.selectedDocument=null; state.searchQuery=""; state.selectedBranch=null; state.searchRun+=1; state.copiedId=""; state.screen="search"; render();});
const dtInput=document.getElementById("dt-query"); if(dtInput){dtInput.addEventListener("input",e=>{state.searchQuery=e.target.value; render(); const ni=document.getElementById("dt-query"); if(ni){ni.focus(); ni.setSelectionRange(state.searchQuery.length,state.searchQuery.length);}});}
const dtSearch=document.getElementById("dt-search-btn"); if(dtSearch) dtSearch.addEventListener("click",()=>{state.searchRun+=1; render();});
document.querySelectorAll("[data-branch]").forEach(btn=>btn.addEventListener("click",()=>{const parts=btn.dataset.branch.split("|||"); state.selectedBranch={lob:parts[0], subLob:parts[1]}; render();}));
const clearBranch=document.getElementById("clear-branch"); if(clearBranch) clearBranch.addEventListener("click",()=>{state.selectedBranch=null; render();});
const backLearning=document.getElementById("back-learning"); if(backLearning) backLearning.addEventListener("click",()=>{state.screen="learning"; render();});
document.querySelectorAll("[data-open-assessment]").forEach(btn=>btn.addEventListener("click",()=>{state.selectedAssessment=assessments.find(a=>a.id===btn.dataset.openAssessment); state.screen="assessmentAttempt"; render();}));
document.querySelectorAll("[data-start-assessment]").forEach(btn=>btn.addEventListener("click",()=>{state.selectedAssessment=assessments.find(a=>a.id===btn.dataset.startAssessment); state.assessmentSelection=""; state.assessmentSubmitted=false; state.screen="assessmentAttempt"; render();}));
const backAssessments=document.getElementById("back-assessments"); if(backAssessments) backAssessments.addEventListener("click",()=>{state.screen="assessments"; render();});
document.querySelectorAll("[data-assessment-option]").forEach(btn=>btn.addEventListener("click",()=>{state.assessmentSelection=btn.dataset.assessmentOption; render();}));
const submitAssessment=document.getElementById("submit-assessment-answer"); if(submitAssessment) submitAssessment.addEventListener("click",()=>{state.assessmentSubmitted=true; render();});
document.querySelectorAll("[data-start-quiz]").forEach(btn=>btn.addEventListener("click",()=>{const quiz=quizSets.find(q=>q.id===btn.dataset.startQuiz); state.quizSession={quiz,index:0,selected:"",score:0,finished:false}; render();}));
document.querySelectorAll("[data-quiz-option]").forEach(btn=>btn.addEventListener("click",()=>{state.quizSession.selected=btn.dataset.quizOption; render();}));
const quizNext=document.getElementById("quiz-next"); if(quizNext) quizNext.addEventListener("click",()=>{const s=state.quizSession; const q=s.quiz.questions[s.index]; const nextScore=s.score+(s.selected===q.answer?1:0); if(s.index===s.quiz.questions.length-1){s.score=nextScore; s.finished=true; s.selected="";} else {s.score=nextScore; s.index+=1; s.selected="";} render();});
const quizBack=document.getElementById("quiz-back-library"); if(quizBack) quizBack.addEventListener("click",()=>{state.quizSession=null; render();});
document.querySelectorAll("[data-select-user]").forEach(row=>row.addEventListener("click",()=>{state.selectedUser=users.find(u=>u.id===row.dataset.selectUser); render();}));
document.querySelectorAll("[data-upload-tab]").forEach(btn=>btn.addEventListener("click",()=>{state.uploadTab=btn.dataset.uploadTab; render();}));
document.querySelectorAll("[data-upload-field]").forEach(el=>{el.addEventListener("input",()=>{state.uploadForm[el.dataset.uploadField]=el.value; render();}); el.addEventListener("change",()=>{state.uploadForm[el.dataset.uploadField]=el.value; render();});});
document.querySelectorAll("[data-registry-filter]").forEach(btn=>btn.addEventListener("click",()=>{state.registryFilter=btn.dataset.registryFilter; render();}));
const dl=document.getElementById("download-ack-report"); if(dl) dl.addEventListener("click",downloadAckReport);}
function downloadAckReport(){const rows=[["Process ID","Process Title","User","Acknowledged At","Status"]]; processDocuments.forEach(proc=>{const ack=acknowledgements.find(a=>a.processId===proc.id); rows.push([proc.id,proc.title,ack?ack.userId:"Pending user acknowledgement",ack?ack.acknowledgedAt:"",ack?ack.status:"Pending"]);}); const csv=rows.map(r=>r.map(val=>`"${String(val).replace(/"/g,'""')}"`).join(",")).join("\n"); const blob=new Blob([csv],{type:"text/csv;charset=utf-8;"}); const a=document.createElement("a"); a.href=URL.createObjectURL(blob); a.download="acknowledgement_report.csv"; a.click(); URL.revokeObjectURL(a.href);}
function handleLogin(){const id=document.getElementById("login-employee").value.trim(); const pw=document.getElementById("login-password").value; const user=users.find(u=>u.id===id&&u.password===pw); const box=document.getElementById("login-error"); if(!user){box.innerHTML=`<div class="error-box">Invalid login details. Use EMP001/demo or ADM001/demo.</div>`; return;} state.user=user; state.screen="dashboard"; state.selectedUser=users[0]; state.selectedDocument=null; state.selectedModule=null; state.selectedAssessment=null; state.searchQuery=""; state.selectedBranch=null; state.searchRun=1; state.copiedId=""; state.quizSession=null; state.assessmentSelection=""; state.assessmentSubmitted=false; render();}
function resetToLogin(){state.user=null; state.screen="dashboard"; state.selectedDocument=null; state.selectedModule=null; state.selectedAssessment=null; state.searchQuery=""; state.selectedBranch=null; state.searchRun=1; state.copiedId=""; state.quizSession=null; state.assessmentSelection=""; state.assessmentSubmitted=false; render();}
render();
